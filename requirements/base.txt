# =============================================================================
# requirements/base.txt
# Core dependencies for usyd-learning (CPU / MPS)
# =============================================================================

# --- Scientific computing ---
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3

# --- NLP / Transformers ---
# torchtext is intentionally excluded: its final release only supports PyTorch
# 2.3, while FLEX's supported CPU/CUDA profiles use PyTorch 2.5 or newer.
transformers>=4.40.0
tokenizers>=0.19.0
datasets>=2.18.0
huggingface_hub>=0.22.0
safetensors>=0.4.0
accelerate>=0.29.0

# --- Visualization ---
matplotlib>=3.8

# --- Utilities ---
PyYAML>=6.0
tqdm>=4.66
Pillow>=10.0
colorama>=0.4.6
packaging>=23.0
torchsummary>=1.5.1
