#-----------------------------
# NN Model — VGG-9 for Tiny-ImageNet (200 classes, 64x64 input)
#-----------------------------
nn_model:
  name: vgg9
  num_classes: 200
  dropout: 0.5
  share_model: True
