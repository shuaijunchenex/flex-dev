#-----------------------------
# Data distribution section (SST2 extreme non-iid)
# Each client owns all samples of a single label
#-----------------------------
data_distribution:

  use: custom

  custom_define:
    custom: [
       [29780, 0],    # Client 1: label 0
       [0, 37569]     # Client 2: label 1
    ]
