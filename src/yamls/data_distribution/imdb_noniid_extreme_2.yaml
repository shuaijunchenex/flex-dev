#-----------------------------
# Data distribution section (IMDB extreme non-iid)
# Each client owns all samples of a single label
#-----------------------------
data_distribution:

  use: custom

  custom_define:
    custom: [
       [12500, 0],    # Client 1: label 0 (neg)
       [0, 12500]     # Client 2: label 1 (pos)
    ]
