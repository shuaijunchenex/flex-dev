#-----------------------------
# Training section args - CoLA training with RoBERTa-large (50 epochs, batch 32)
#-----------------------------
training:
  epochs: 50
  batch_size: 32
  shuffle: True
  lr: 0.0001
  save_path: "./.training_results/"
